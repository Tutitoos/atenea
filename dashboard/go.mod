// This marker keeps Go's parent-module ./... traversal out of frontend
// dependencies that contain incidental Go examples (for example flatted).
module github.com/Tutitoos/atenea-dashboard

go 1.25.13
