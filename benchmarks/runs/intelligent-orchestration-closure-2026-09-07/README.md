# Intelligent orchestration operational closure

This artifact records P19-P29 without raw transcripts, credentials, private paths, thread identifiers, or request identifiers. Issue #48 and PR #47 are the public delivery anchors.

The GitHub delivery skill passed behavioral and structural validation plus independent Sol review. Four Codex invocations requested the fixed role profiles and efforts and returned usage. The client did not expose observed model or effort identity, so both remain unknown and this evidence is partial. The four probes used 63,745 input tokens, including 8,960 cached input tokens, and 52 output tokens. Provider price remains unknown.

Codex CLI 0.153.4 exposed `thread/fork` in its generated App Server schema. The provider-real gate completed a minimal parent turn and created a distinct child in 9.81 seconds. Productive integration now reserves the external effect, persists the child before dispatch, and blocks automatic retry after an uncertain fork; exact-head independent review is still pending. This validates the client operation without claiming access to Codex's model-internal collaboration tools.

Client-real evidence remains intentionally mixed. Claude Code connected to the ATENEA MCP server inside the isolated launcher before its isolated HOME lacked model authentication. OMP 18.0.11 passed the repository's isolated real integration. Codex and OpenCode did not complete the isolated client gate. ChatGPT and OMP lack generic safe overlay injection, so presentation remains manual or unknown. Direct MCP server probes passed for every detected client entry; a server probe does not prove that client used the tool or rendered progress correctly.

P26-P29 are updated only after checks, exact-head review, audit, merge, installed-binary smoke tests, and the final published report complete.
